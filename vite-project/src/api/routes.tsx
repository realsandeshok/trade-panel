const Base_URL ="http://localhost:3000";

const GetUser = `${Base_URL}/api/users`;
const UserLogin = `${Base_URL}/api/login`;
const UserSignup = `${Base_URL}/api/signup`;

export {GetUser, UserLogin, UserSignup}

